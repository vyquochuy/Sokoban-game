Hướng dẫn chạy source code

từ thư mục gốc, di chuyền vào thư mục source, thư mục chứa mã nguồn của game sau đó chạy thuật toán từ 
terminal bằng lệnh python menu.py
giao diện menu sẽ hiện lên với thuật toán mặc định là BFS và có thể lựa chọn thay đổi
sau đó ấn nút bắt đầu với BFS (thuật toán đã lựa chọn) để chọn testcase ứng với từng file input

cửa sổ giao diện bản đồ sẽ hiện lên và Ares sẽ tự tìm đường đi dựa vào thuật toán đã chọn, nếu không thể
tìm được đường đi thì màn hình sẽ hiện lên dòng chữ "UNSOLVABLE WITH CURRENT ALGORITHM" và tự động tắt
sau khi tìm được đường đi và giải xong bài toán, Ares sẽ dừng lại và màn hình hiện lên nút close để tắt
cửa sổ bản đồ hiện tại